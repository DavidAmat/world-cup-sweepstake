Never-built explorations and historical records — no banner.

Plans and implementation journals from hitos 02–16 live here (renamed from legacy `context-old/`). Current docs are under `context/` and `documentation/services/`.
