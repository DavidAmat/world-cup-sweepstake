Read the files in 01-brainstorming-prompt.md and 02-pid.md and write a new file 01-plan.md detailing the implementation plan. Since it is very large, this 01-plan.md will be like a summarizer of the different files you will specifically create for each milestone of the plan. so you will create inside documentation/archive/ ad 02-XXX.md, 03-XXX.md according to the different features we want to start working on (in order) so that the 01-plan.md will have a short summary of what file does what, this way we will have a knowledge base of the implementation plan. You are not going to create any files yet, only organize all the knowledge into a plan that details technical decisions, infrastructure, high level skeletons of tables or scripts (not entering into much detail about the whole scripts but it is useful to create simple skeletons of the code to guide the later implementation). For the implementation I will create a context/implementations folder where you will put the progress summary, errors, decisions, changes of direction, etc... but this is only when we start implementing and testing. Your task is to now focus on the plan of implementation, from local development and local testing, to set up the production infrastructure, test it with the dummy data of catar 2022 (you will need a moment in which I will have to manually add the results of a given "jornada", it is ok)

Remember language of the UI will be in Spanish, users are spanish, not english. You can create tables in Supabase in english but bear in mind that the UI will be in spanish. 